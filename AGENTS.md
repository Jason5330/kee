# AGENTS.md

這個專案的完整規則、架構、操作流程都寫在同目錄的 [`CLAUDE.md`](./CLAUDE.md) 裡——
不要只看這份檔案就動作，先完整讀 `CLAUDE.md`，那份才是唯一權威來源（single source of truth）。

這份 `AGENTS.md` 存在的唯一目的：Codex CLI（以及其他遵循 [agents.md](https://agents.md)
開放標準的工具）預設會找這個檔名，所以留一個指標檔讓它們也能定位到規則入口，
避免同一套規則要維護兩份、內容跑掉（drift）。

如果你是 Codex：讀完這行之後，立刻去讀 `CLAUDE.md` 的全文，把它當成你在這個目錄下的行為準則。
