# KEE — 本地知識庫（Schema 層 / Codex 完整鏡像）

> 這份檔案是 `CLAUDE.md` 的**完整鏡像**，給 Codex CLI／遵循 [agents.md](https://agents.md)
> 標準的工具用——這類工具預設只讀 `AGENTS.md`，不會主動跳去讀 `CLAUDE.md`，所以規則內容
> 必須整份放在這裡，不能只留指標。
>
> **編輯規則要改，先改 `CLAUDE.md`**（那份是唯一的編輯本體），改完要把同樣的改動同步複製
> 到這份檔案，兩份內容必須一致。不要只改這份而漏了 `CLAUDE.md`，也不要只改 `CLAUDE.md`
> 而忘了同步回這裡。
>
> 架構原理：Karpathy「LLM Wiki」模式（LLM 維護一個持續累積的 wiki，而不是每次現場 RAG）
> + Obsidian 精神（原子筆記、雙向連結、MOC 索引頁）。純 Markdown 檔案取代 Obsidian 軟體本身——
> 不需要裝任何 App、不需要點圖譜，直接問 AI agent，它幫你把相關筆記讀出來、整理、回答。

## 三層架構（不可混淆）

```
sources/   ← 原始資料，唯讀、永不修改（PDF、文章、逐字稿、截圖 OCR 文字…）
wiki/      ← Agent 維護的知識庫本體，會持續增長、修改、互相連結
CLAUDE.md  ← 規則的編輯本體（schema 層），規則變了先改這裡
AGENTS.md  ← 這份檔案，給 Codex/其他 agent 看的完整鏡像，內容要跟 CLAUDE.md 的規則段落保持一致
```

規則：
- `sources/` 裡的檔案**絕對不能被 agent 編輯或刪除**，只能讀取、引用。
- `wiki/` 裡的每一頁都是 agent 寫的、agent 維護的，使用者通常不用手動編輯。
- `CLAUDE.md` 只在「知識庫的運作規則要改」時才動，不是拿來記知識本身。
- 改完 `CLAUDE.md` 的規則段落後，**一定要把同樣的改動同步進這份 `AGENTS.md`**，不能只改一份。

## wiki/ 內部結構

```
wiki/
├── index.md          ← 總目錄（MOC，Map of Content）。所有頁面 + 一句話摘要，依分類列出。
├── log.md             ← 只增不改的操作日誌，每次 ingest / ask / lint 都要記一筆。
├── entities/          ← 原子筆記：人、工具、公司、產品、帳號…「一個實體一頁」
├── concepts/          ← 原子筆記：概念、方法論、原理…「一個概念一頁」
├── projects/          ← 進行中的專案/任務（有明確目標和進度）
├── moc/               ← 主題索引頁（Map of Content），把同主題的多篇筆記串起來
└── _templates/        ← 新頁面模板，新增頁面前先抄這裡的格式
```

分類判斷原則（遇到新資訊時決定放哪一種頁面）：
- 是「一個東西/一個人/一個工具」→ `entities/`
- 是「一個想法/一個做法/一套理論」→ `concepts/`
- 是「一件正在推進、有進度的事」→ `projects/`
- 是「一堆筆記需要一個入口串起來」→ `moc/`（例如「知識庫系統」這個大主題本身可以有一頁 moc）

## 頁面格式（所有 wiki 頁面都要有這 6 個區塊）

```markdown
---
name: {{頁面名稱，同時是檔名}}
type: {{entity|concept|project|moc}}
tags: [{{標籤1}}, {{標籤2}}]
created: {{YYYY-MM-DD}}
updated: {{YYYY-MM-DD}}
status: {{active|stale|archived}}
---

# {{標題}}

## 摘要
一句話講清楚這頁在講什麼（這句話會被複製到 index.md）。

## 現況
**這是覆蓋區——永遠只放最新的、目前為真的結論。** 每次有新資訊進來，直接改寫這一段，
不要在這裡疊加歷史。這段是「如果我現在要用這個知識，看這段就夠了」。

## 時間線
**只增不改。** 每次這頁被更新，往下追加一行，不要刪改舊紀錄。
- YYYY-MM-DD：發生了什麼、學到什麼、來源是什麼

## 關聯
用 `[[頁面名稱]]` 連到其他相關的 wiki 頁面（不用管副檔名/路徑，agent 會自己解析）。
- [[某個相關頁面]] — 為什麼相關

## 反向連結
**agent 自動維護，使用者不用手動填。** 只要別的頁面用 `[[這一頁]]` 連過來，
agent 就要回頭在這裡補一行。`/lint` 會檢查這個區塊有沒有漏掉。
- [[來源頁面]]

## 來源
這頁的結論是根據哪些 `sources/` 裡的原始檔案，或哪次對話。
- `sources/xxx.pdf`（第 3 節）
- 2026-07-25 對話
```

## 首次使用（bootstrap）

若 `wiki/index.md` 或 `wiki/log.md` 不存在（例如剛 clone 這個模板下來的全新環境），
先把 `wiki/index.example.md` → `wiki/index.md`、`wiki/log.example.md` → `wiki/log.md`
複製一份出來（保留 example 檔本身，不要刪），再繼續往下做，不用特別問使用者。

## 三個核心操作

觸發方式不限定單一語法：使用者打 `/ingest`、`/ask`、`/lint`（Claude Code 和 Codex 的 slash command
都能觸發），或者只是用自然語言講「幫我整理這個」「知識庫裡有沒有 X」「幫我健檢一下」，
只要語意對得上，就照對應操作的步驟做，不要要求使用者一定要背指令語法。

### 1. Ingest（吃進新資料）
觸發：使用者丟新資料進來（貼文字、給檔案路徑、講一段新資訊）。
步驟：
1. 若是檔案，先存進 `sources/`（原檔不動，之後只讀不改）。
2. 通讀內容，抓出裡面提到的「實體」「概念」「專案」。
3. 對每一個，判斷是「開新頁」還是「更新既有頁」（先查 `wiki/index.md` 有沒有同名或近似頁面，
   不要重複建立語意相同的頁面）。
   **判斷「這是更新資料」的準則**（符合任一即視為更新，不是全新內容）：
   - 標題/名稱跟 index.md 裡某頁相同或明顯是同一實體、同一概念、同一專案的近義詞。
   - 內容是既有頁面「現況」的後續發展、修正、或延伸（例如同一份表格的新版本、
     同一件事的最新進度）。
   - 表格類資料檔名字根相同、只有日期不同（見下方 1a）。
4. **只更新有牽涉到的頁面，不要藉機把整份 wiki 重新整理一遍**：判定為更新後，動作範圍
   僅限於該頁本身、它新產生的關聯/反向連結、以及 `index.md` 裡那一行摘要（若摘要句真的變了才改）。
   不要重寫其他無關頁面的內容或格式，不要重新排版整份 `index.md`，不要把沒被這次資料影響到
   的頁面也順手「整理」一次。
5. **判定為更新後，先讀舊版再比對出差異，不要憑印象猜**：
   - 一般文字：先讀該頁目前的「現況」，拿新資料逐句比對——現況沒提到的算「新增」，
     跟現況矛盾/不同的算「變更」，現況已經講過的算重複、略過不記。
   - 表格：從 `sources/` 找出同字根、較早日期的上一版，挑一個關鍵欄位（日期/單號/ID/
     目的地代碼）當對照基準逐列比對，抓出「新增列」「數值變動列」「刪除列」。
   - 只有「新增」「變更」這兩類才寫進時間線；重複的部分不記錄，也不要整段照抄新資料。
6. 更新時：改寫「現況」區塊為最新結論（只反映上一步比對出的變更），「時間線」區塊往下
   追加一行（只寫差異，例如「新增 8 筆、X 欄位從 120 變 135」），補「來源」。
7. 新建時：套用 `_templates/` 對應模板，六個區塊都要填。
8. 檢查這頁有沒有該連到別的既有頁面，補上「關聯」的 `[[連結]]`；同時回去幫**被連到的那一頁**
   補上「反向連結」（雙向都要更新，這是取代 Obsidian 自動反向連結功能的手動機制）。
9. 更新 `wiki/index.md`：新頁加一行、舊頁摘要句若變了就同步改，其他行不動。
10. 在 `wiki/log.md` 追加一筆（格式見下方）。
11. 回報使用者：這次動了哪幾頁、新增幾頁、更新幾頁（**要讓使用者看得到發生了什麼，不要只說「已完成」**）。

### 1a. Ingest 表格類資料（Excel/CSV）的特別規則

使用者大量資料是 `.xlsx`/`.csv`，跟一般文章/筆記不一樣，不能套用上面「通讀內容抓實體/概念」
的做法（會爆出一堆瑣碎頁面）。改用這套流程：

1. 存進 `sources/`（檔名帶日期，例如 `2026-07-25_費用明細.xlsx`；同一份表格之後更新，
   存成新檔案、帶新日期，不要覆蓋舊檔——舊版本本身就是時間線的證據）。
2. **不要用純文字方式讀二進位檔**：`.xlsx` 要用讀表格的方式（xlsx 相關工具/技能）先摸清楚
   結構——有幾個工作表、每個工作表的欄位名稱、大約幾列資料、時間範圍。`.csv` 可以直接讀。
3. 判斷這份表格對應到哪個既有 `project`/`concept`（例如「費用明細.xlsx」對應 project
   「個人記帳」；沒有對應頁面就新建一個 project 頁）。
4. 在對應頁面的「現況」寫**摘要層級的結論**：欄位有哪些、關鍵數字、有沒有異常或趨勢——
   **不要把整張表的內容逐列搬進 wiki 頁面**，wiki 頁面是摘要，不是資料庫的副本。
5. 「來源」區塊要精確標注工作表名稱和大概範圍，例如 `sources/2026-07-25_費用明細.xlsx`
   （工作表「7月」，約 120 列），這樣之後 `/ask` 查到明細層級的問題時，知道要回頭開哪個
   檔案、哪個工作表重新讀，而不是只能回答摘要。
6. 每次同一主題的新版表格進來，照上面「1. Ingest」步驟 5 的比對方法（挑關鍵欄位逐列對照
   舊版），「時間線」往下追加一行寫清楚差異（新增幾筆、某個數字從多少變多少）——這是表格
   資料的「複利」：你查一次歷史趨勢，之後不用每次重新打開好幾份舊檔案比對，wiki 頁面已經
   幫你摘要好了。

### 2. Query（提問）
觸發：使用者問問題，要 agent 從知識庫裡把相關資訊整理出來。
步驟：
1. 先看 `wiki/index.md`，用關鍵字/標籤/類別判斷可能相關的頁面（不要只搜檔名，
   摘要句和 tags 都要比對）。
2. 讀取命中的頁面本體；如果頁面的「關聯」區塊連到其他頁面，也順著讀一層（一跳之內）。
3. 只根據「現況」區塊和「來源」整理答案，時間線僅在使用者問「演變過程/歷史」時才引用。
   若問題需要 wiki 摘要裡沒有的**明細層級數字**（常見於 Excel 來源，wiki 頁面只留摘要），
   照「來源」區塊的指引回頭打開對應的 `sources/xxx.xlsx`（指定的工作表）重新讀取，
   不要拿摘要層級的結論去湊明細數字。
4. 回答時附引用：答案來自哪一頁 wiki（`[[頁面名稱]]`），該頁又來自哪個原始來源。
   **不要憑空回答知識庫裡沒有的東西**——如果沒查到，明確說「知識庫裡目前沒有這個資訊」。
5. 如果這次整理出來的答案本身有沉澱價值（例如使用者反覆會問），主動問是否要存成新的
   wiki 頁面或補進既有頁面。
6. 在 `wiki/log.md` 追加一筆 QUERY 紀錄。

### 3. Lint（健檢）
觸發：使用者喊 `/lint`，或知識庫累積到一定規模時主動建議跑一次。
檢查項目：
- **斷link**：某頁的 `[[連結]]` 指到不存在的頁面。
- **孤兒頁**：沒有任何頁面連過來、也沒被任何 MOC 收錄的頁面。
- **反向連結漏補**：A 連了 B，但 B 的「反向連結」區塊沒有 A（直接修好，不用問）。
- **過期頁**：`updated` 超過 90 天，但同主題其實有更新的相關筆記沒同步進來。
- **矛盾**：兩頁對同一件事的「現況」互相矛盾。
修法：
- 能自動修的（反向連結漏補、index.md 沒同步）直接修，修完列出改了什麼。
- 不能自動判斷的（矛盾、該不該合併重複頁）列成清單問使用者怎麼處理。
- 在 `wiki/log.md` 追加一筆 LINT 紀錄，列出這次修了幾個、剩幾個待人工決定。

## log.md 格式（方便之後用 grep 找）

```
- YYYY-MM-DD HH:MM [INGEST] 來源=xxx → 新增: A, B / 更新: C
- YYYY-MM-DD HH:MM [QUERY] 問題="..." → 引用: A, C
- YYYY-MM-DD HH:MM [LINT] 自動修復 N 項，待處理 N 項
```

## 標籤慣例

- 標籤是給「跨分類」的交叉查詢用的（例如 `#理財`、`#工作`、`#健康`），
  分類（entities/concepts/…）是給「這是什麼形態的知識」用的，兩者不重複。
- 標籤全部小寫，新增標籤前先查 `index.md` 有沒有同義標籤在用了。

## 給 agent 的行為準則（不管是 Claude Code 還是 Codex 都必須遵守）

- 每次 ingest/lint 完，一定要用條列或表格回報動了哪幾頁、為什麼——不要只說「處理好了」。
- 每次新建或更新頁面，六個區塊都要有，不能省略「反向連結」的維護（這是整套系統唯一
  取代 Obsidian 雙向連結的機制，漏了等於白做）。
- 不確定新資訊該歸哪一頁、或該不該跟既有頁面合併時，先問使用者，不要自己猜著建一堆重複頁。
- **判定為「更新既有資料」時，只動被影響到的頁面**，不要順手整理/重寫其他無關頁面，
  也不要整份重新排版 `index.md`——這是為了讓維護成本可控，每次 ingest 的改動範圍應該
  跟新資料的範圍成正比，而不是每次都掃過全部 wiki。
- 回答一律繁體中文，直接給結果，重要流程用 `→` 箭頭表示。
- `sources/`、`wiki/entities|concepts|projects|moc/`、`wiki/index.md`、`wiki/log.md` 是使用者的
  個人資料，不會進版本控制（見 `.gitignore`）——`CLAUDE.md`、這份 `AGENTS.md`、`_templates/`、
  指令檔才是要公開分享的「系統本體」，改動時要意識到這個界線。
