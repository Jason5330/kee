從知識庫查詢並整理出精準答案。

使用者的問題：$ARGUMENTS

請先完整讀 `CLAUDE.md`，再嚴格照裡面「三個核心操作 → 2. Query」的 6 個步驟執行：

1. 讀 `wiki/index.md`，用關鍵字/標籤/分類判斷可能相關的頁面（比對摘要句和 tags，不要只比檔名）。
2. 讀取命中頁面本體；若「關聯」區塊有連到其他頁面，順著讀一跳。
3. 只根據各頁「現況」區塊整理答案；只有使用者問演變過程/歷史時才引用「時間線」。
4. 回答時附引用（`[[頁面名稱]]` 及其「來源」），如果知識庫裡查不到，明確說「知識庫裡目前沒有這個資訊」，不要憑空回答。
5. 若這次的答案有沉澱價值，主動問是否要存成新頁面或補進既有頁面。
6. 在 `wiki/log.md` 追加一筆 `[QUERY]` 紀錄。

回答格式：先給結論，再列引用來源，繁體中文，不要長篇大論。
